<UserControl x:Class="PiaNewbie.Views.PracticePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrl="clr-namespace:PiaNewbie.Controls"
             Background="#050505"
             Focusable="True">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Foreground="#FFFFFF" FontSize="14"
                       Text="{Binding StatusText}" HorizontalAlignment="Left" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center"
                        Visibility="{Binding ShowRhythmStats, Converter={StaticResource BoolToVis}}">
                <TextBlock Foreground="#4ADE80" FontSize="14" FontWeight="SemiBold"
                           Text="{Binding RhythmHitCount, StringFormat=HIT {0}}"/>
                <TextBlock Foreground="#F87171" FontSize="14" FontWeight="SemiBold" Margin="16,0,0,0"
                           Text="{Binding RhythmMissCount, StringFormat=MISS {0}}"/>
                <TextBlock Foreground="#FCD34D" FontSize="14" FontWeight="SemiBold" Margin="16,0,0,0"
                           Text="{Binding ComboCount, StringFormat=콤보 {0}}"/>
            </StackPanel>

            <TextBlock Grid.Column="2" Foreground="#9CA3AF" FontSize="14" FontFamily="Consolas"
                       Text="{Binding PlaybackClockText}" Margin="16,0,0,0"
                       HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>

        <ctrl:PianoRollWebViewControl x:Name="Roll" Grid.Row="1" Margin="8,0"/>

        <TextBlock Grid.Row="2" Text="ESC: 설정으로 · F5: 즉시 재시작"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="12" Foreground="#6B7280" Margin="0,12,0,16"/>
    </Grid>
</UserControl>
